spring:
  application:
    name: eureka
#server:
#  port: 8761
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
#      这里如果关闭了这个，组织eureka注册自身，那么eureka集群将不可用（其他eureka获取不到注册到这个eureka的信息）
#    register-with-eureka: false
  server:
    enable-self-preservation: false

